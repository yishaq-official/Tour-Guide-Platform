/**
 * MapWidget Facade
 * 
 * Re-exports LeafletMapView from shared/components/map
 * for backwards compatibility with existing page imports.
 */

export { LeafletMapView as MapWidget } from "../shared/components/map/LeafletMapView";